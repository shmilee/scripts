diff --git a/src/lib/live.ts b/src/lib/live.ts
index fa4a5e9..8477d0b 100644
--- a/src/lib/live.ts
+++ b/src/lib/live.ts
@@ -3,6 +3,7 @@
 
 import { getConfig } from "@/lib/config";
 import { db } from "@/lib/db";
+import { completeCollectionPath } from '@/lib/config.collections';
 
 const defaultUA = 'AptvPlayer/1.4.10';
 const TVBOX_UA = 'okhttp/4.1.0';
@@ -135,7 +136,7 @@ export async function refreshLiveChannel
   try {
     // 第一次 Fetch - 使用超时控制
     console.log(`[Live] Fetching URL: ${liveInfo.url} with UA: ${isTvBox ? TVBOX_UA : ua}`);
-    const response = await fetchWithTimeout(liveInfo.url, {
+    const response = await fetchWithTimeout(completeCollectionPath(liveInfo.url, null), {
       headers: {
         'User-Agent': isTvBox ? TVBOX_UA : ua,
       },
@@ -299,7 +300,7 @@ async function processTvBoxContent(conte
       const liveUa = firstLive.ua || TVBOX_UA;
 
       try {
-        const response = await fetch(firstLive.url, {
+        const response = await fetch(completeCollectionPath(firstLive.url, null), {
           headers: {
             'User-Agent': liveUa
           }
@@ -494,7 +495,7 @@ async function parseEpg(
   try {
     // 🚀 优化：使用超时控制
     console.log(`[Live] Fetching EPG from ${epgUrl} with ${FETCH_TIMEOUT}ms timeout...`);
-    const response = await fetchWithTimeout(epgUrl, {
+    const response = await fetchWithTimeout(completeCollectionPath(epgUrl, null), {
       headers: { 'User-Agent': ua },
     }, FETCH_TIMEOUT);
 
