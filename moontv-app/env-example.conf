## 必填：站长账号和密码，任意字符串
ADMIN_USERNAME=${ADMIN_USERNAME:-admin}
PASSWORD=${PASSWORD:-admin_password}

## 必填：播放记录/收藏的存储方式，redis、kvrocks、upstash、valkey，无默认
##   valkey: local redis replacement server
NEXT_PUBLIC_STORAGE_TYPE=${NEXT_PUBLIC_STORAGE_TYPE:-valkey}
#REDIS_URL=${REDIS_URL:-}
#KVROCKS_URL=${KVROCKS_URL:-}
#UPSTASH_URL=${UPSTASH_URL:-'Your HTTPS ENDPOINT'}
#UPSTASH_TOKEN=${UPSTASH_TOKEN:-'Your TOKEN'}
#VALKEY_SRVCMD=${VALKEY_SRVCMD:-valkey-server}
#VALKEY_CLICMD=${VALKEY_CLICMD:-valkey-cli}
VALKEY_PORT=${VALKEY_PORT:-16379}

## 可选：站点监听端口
PORT=${PORT:-3000}
## 可选：站点 URL，如 https://example.com，无默认
#SITE_BASE=${SITE_BASE:-}
#HOSTNAME=${HOSTNAME:-192.168.0.53}

## 可选：站点名称，任意字符串
#NEXT_PUBLIC_SITE_NAME=${NEXT_PUBLIC_SITE_NAME:-MoonTV}
## 可选：站点公告，任意字符串
#ANNOUNCEMENT=${ANNOUNCEMENT:-'本网站仅提供...不负责。'}

# 可选：豆瓣代理配置
# 代理类型
#NEXT_PUBLIC_DOUBAN_PROXY_TYPE=${NEXT_PUBLIC_DOUBAN_PROXY_TYPE:-direct}
# 自定义代理 URL prefix
#NEXT_PUBLIC_DOUBAN_PROXY=${NEXT_PUBLIC_DOUBAN_PROXY:-}
# 图片代理类型
#NEXT_PUBLIC_DOUBAN_IMAGE_PROXY_TYPE=${NEXT_PUBLIC_DOUBAN_IMAGE_PROXY_TYPE:-direct}
# 自定义图片代理 URL prefix
#NEXT_PUBLIC_DOUBAN_IMAGE_PROXY=${NEXT_PUBLIC_DOUBAN_IMAGE_PROXY:-}

## 可选：搜索
# 最大页数，1-50
#NEXT_PUBLIC_SEARCH_MAX_PAGE=${NEXT_PUBLIC_SEARCH_MAX_PAGE:-5}
# 流式输出，true/false
#NEXT_PUBLIC_FLUID_SEARCH=${NEXT_PUBLIC_FLUID_SEARCH:-true}

## 可选：M3U8 代理 TOKEN
NEXT_PUBLIC_M3U8_URL_TOKEN=${NEXT_PUBLIC_M3U8_URL_TOKEN:-12345qwert}

## 可选：咪咕直播
MIGU_UPDATE=${MIGU_UPDATE:-TV}
#MIGU_USERID=${MIGU_USERID:-18xxxxxxxx}
#MIGU_TOKEN=${MIGU_TOKEN:-nlps1111XXXX1111XXXX1111}

## 可选：其他
NODE_ENV=${NODE_ENV:-production}
# 关闭色情内容过滤
#NEXT_PUBLIC_DISABLE_YELLOW_FILTER=${NEXT_PUBLIC_DISABLE_YELLOW_FILTER:-false}
# 更新 config-collections 中的订阅源
#UPDATE_COLLECTIONS=${UPDATE_COLLECTIONS:-false}

## 可选：v1d 用户注册
# 是否开启用户注册
#NEXT_PUBLIC_ENABLE_REGISTRATION=${NEXT_PUBLIC_ENABLE_REGISTRATION:-false}
# 注册用户默认分配的用户组
#DEFAULT_REGISTRATION_GROUP=${DEFAULT_REGISTRATION_GROUP:-}

## 可选：v2p https://github.com/mtvpls/MoonTVPlus#环境变量
# 定时任务 API 访问密码
#CRON_PASSWORD=${CRON_PASSWORD:-mtvpls}
# M3U8 代理 API 鉴权 Token（外部播放器跳转时的鉴权token，不填为无鉴权）
#NEXT_PUBLIC_PROXY_M3U8_TOKEN=${NEXT_PUBLIC_PROXY_M3U8_TOKEN:-}
# 弹幕缓存失效时间（分钟数，设为 0 时不缓存）
#NEXT_PUBLIC_DANMAKU_CACHE_EXPIRE_MINUTES=${NEXT_PUBLIC_DANMAKU_CACHE_EXPIRE_MINUTES:-4320}
# 是否启用 TVBOX 订阅功能
#ENABLE_TVBOX_SUBSCRIBE=${ENABLE_TVBOX_SUBSCRIBE:-false}
#TVBOX_SUBSCRIBE_TOKEN=${TVBOX_SUBSCRIBE_TOKEN:''}
#TVBOX_BLOCKED_SOURCES=${TVBOX_BLOCKED_SOURCES:-}
# 是否启用观影室功能（vercel部署不支持该功能，可使用外部服务器）
#WATCH_ROOM_ENABLED=${WATCH_ROOM_ENABLED:-false}
#WATCH_ROOM_SERVER_TYPE=${WATCH_ROOM_SERVER_TYPE:-internal}
#WATCH_ROOM_EXTERNAL_SERVER_URL=${WATCH_ROOM_EXTERNAL_SERVER_URL:-'WebSocket URL'}
#WATCH_ROOM_EXTERNAL_SERVER_AUTH=${WATCH_ROOM_EXTERNAL_SERVER_AUTH:-}
#NEXT_PUBLIC_VOICE_CHAT_STRATEGY=${NEXT_PUBLIC_VOICE_CHAT_STRATEGY:-webrtc-fallback}
# 是否启用服务器离线下载功能（开启后也仅管理员和站长可用）
#NEXT_PUBLIC_ENABLE_OFFLINE_DOWNLOAD=${NEXT_PUBLIC_ENABLE_OFFLINE_DOWNLOAD:-false}
#OFFLINE_DOWNLOAD_DIR=${OFFLINE_DOWNLOAD_DIR:-/data}
# 私人影库视频信息在内存中的缓存时长（分钟）
#VIDEOINFO_CACHE_MINUTES=${VIDEOINFO_CACHE_MINUTES:-1440}
# 是否开启源站寻片功能
#NEXT_PUBLIC_ENABLE_SOURCE_SEARCH=${NEXT_PUBLIC_ENABLE_SOURCE_SEARCH:-}
# 单个用户播放记录清理阈值（超过此数量将自动清理旧记录）
#MAX_PLAY_RECORDS_PER_USER=${MAX_PLAY_RECORDS_PER_USER:-100}
# 初始配置（JSON 字符串，包含 api_site、custom_category、lives 等）
#INIT_CONFIG=${INIT_CONFIG:-}
# 配置订阅 URL（Base58 编码的配置文件地址，优先级高于 INIT_CONFIG）
#CONFIG_SUBSCRIPTION_URL=${CONFIG_SUBSCRIPTION_URL:-}
# TMDB API 密钥
#TMDB_API_KEY=${TMDB_API_KEY:-}
# TMDB 代理地址
#TMDB_PROXY=${TMDB_PROXY:-}
# TMDB 反向代理地址
#TMDB_REVERSE_PROXY=${TMDB_REVERSE_PROXY:-}
# 弹幕 API 地址
#DANMAKU_API_BASE=${DANMAKU_API_BASE:-'http://localhost:9321'}
# 弹幕 API Token
#DANMAKU_API_TOKEN=${DANMAKU_API_TOKEN:-'87654321'}

## 可选：v5s 禁用首页预告片
#DISABLE_HERO_TRAILER=${DISABLE_HERO_TRAILER:-false}
